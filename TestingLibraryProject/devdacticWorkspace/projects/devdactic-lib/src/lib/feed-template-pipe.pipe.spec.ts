import { FeedTemplatePipePipe } from './feed-template-pipe.pipe';

describe('FeedTemplatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FeedTemplatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
