import { FeedTemplatePipe } from './feed-template.pipe';

describe('FeedTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new FeedTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
